query Cards($query: String!, $game: GameCode!) {
    cards(query: $query, game: $game) {
        code
        jpName
        rarity
        price
        currency {
            code
            numericCode
            fraction
            grapheme
            template
            decimal
            thousand
        }
        image
        score
        game {
            code
        }
    }
}